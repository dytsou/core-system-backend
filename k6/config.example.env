# k6 Test Configuration Example
# Copy this file to .env and fill in your values
#
# NOTE: The scripts automatically create test data (orgs, units, forms, questions)
# during setup. You only need to provide valid user UUIDs.

# Backend API base URL
BASE_URL=http://localhost:8080

# User UUIDs for authentication (comma-separated)
# These MUST exist in the database
# Run: SELECT id, name FROM users; to find valid UUIDs
USER_UUIDS=00000000-0000-0000-0000-000000000001,00000000-0000-0000-0000-000000000002

# Admin UUIDs for admin operations (comma-separated)
# Use the same UUIDs as USER_UUIDS or users with admin privileges
ADMIN_UUIDS=00000000-0000-0000-0000-000000000001

# Member emails for member management tests
# These users must exist in the database
# Run: SELECT value FROM user_emails; to find valid emails
MEMBER_EMAILS=member1@example.com,member2@example.com
